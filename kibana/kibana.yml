server.port: 5601
server.host: "0.0.0.0"

elasticsearch.hosts: ["http://es:9200"]

logging:
  appenders:
    default:
      type: console
      layout:
        type: pattern
  root:
    appenders: [default]
    level: info